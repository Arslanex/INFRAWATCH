server {
    listen 80;
    server_name static.test;

    root /var/www/static.test;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }

    location /assets {
        alias /var/www/static.test/dist;
        expires 30d;
    }
}
