server {
    listen 80;
    server_name nested.test;

    location /api {
        proxy_pass http://127.0.0.1:9000;

        location /api/health {
            access_log off;
            proxy_pass http://127.0.0.1:9001;
        }
    }
}
